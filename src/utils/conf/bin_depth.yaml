data_directory : "/home/epon04yc/Orbbec/build/bin/"
experiment_name : "test_overlay" # Name of the experiment
input_directory : "${data_directory}/${experiment_name}"
output_directory : "${data_directory}/${experiment_name}"
shape : [1080, 1920]